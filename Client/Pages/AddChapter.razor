@page "/addchapter/{BookId:guid}"
@using ThePenfolio.Client.Shared.Components

@if (!isLoading)
{
    if (userId != null)
    {
        <ChapterForm Chapter="@chapter" IsSubmitting="@isSubmitting" OnSubmit="OnSubmit" />
    }
    else
    {
        <NotLoggedIn />
    }
}